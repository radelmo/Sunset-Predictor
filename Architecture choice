We chose the Node.js, Pug setup because it was displayed in class, and we are interested in learning a stack that is in Javascript as the
progessor has pointed out its a very simple stack. Also, javascript is wicked common so we want to know it better.
